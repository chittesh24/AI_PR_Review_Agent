# GitHub App credentials (do NOT commit your private key)
APP_ID=123456
INSTALLATION_ID=7891011
PRIVATE_KEY_PATH=/app/private-key.pem

# Repository and PR to run against
GITHUB_REPOSITORY=chittesh24/AI_PR_Review_Agent
PR_NUMBER=1

# Ollama host reachable from inside Docker
OLLAMA_HOST=http://host.docker.internal:11434

# Optional: model name if different
OLLAMA_MODEL=llama3
